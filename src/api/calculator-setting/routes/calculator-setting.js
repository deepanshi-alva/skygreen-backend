'use strict';

/**
 * calculator-setting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::calculator-setting.calculator-setting');
